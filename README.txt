Tento dokument je pokynem k použití "prgsem" *(a "my_comp_module").

-->) prgsem: ovladací aplikace. 
Je inspirovaná video tutorialem, ale byla modifikovaná.

Je protestovaná na: 
1) různých hodnotách rozměru okna, (extra)
2) různých minimalních a maximalních hodnotáh, (extra) 
3) taky na  různých hodnotáh parametru c. (extra)
Jsou realizovaný podle pokynu na BRUTE akce a jejich klavesy ("s", "c", "a", "g","q").
K použití různých parametu je napsán terminalní textový interface.

-->) my_comp_module: vypočetní aplikace.
Je inspirovaná již modifikovanou vypočetní aplikaci.
Již (doufám) neobsahuje chyby a memory leaks v porovnání s první verzi.

Tento program obsahuje uvedené výš (extra) funkcionality, ale neobsahuje bonusové.

@Kushnerenko Matvii 

